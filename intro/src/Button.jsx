const Button = () => {
  return (
    <button
      style={{
        padding: "1rem",
        background: "black",
        color: "white",
        cursor: "pointer",
      }}
    >
      Click Me
    </button>
  );
};

export default Button;
